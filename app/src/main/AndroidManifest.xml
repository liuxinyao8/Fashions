<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fashions">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".apps.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/loggion"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/loggion"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.HomeTable"
            android:theme="@style/MyNoAppTheme"
            ></activity>
        <activity android:name=".ui.RegistActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity
            android:name=".ui.Homerv2"
            android:theme="@style/MyNoAppTheme" />
        <activity android:name=".ui.Rv2Activity" />
        <activity
            android:name=".ui.Rv1Activity"
            android:theme="@style/MyNoAppTheme" />
        <activity
            android:name=".ui.Rev4_Activity"
            android:theme="@style/MyNoAppTheme" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyNoAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>